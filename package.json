{
  "name": "solsocket-monorepo",
  "private": true,
  "description": "Socket.io-style realtime multiplayer SDK for Solana, powered by MagicBlock Ephemeral Rollups",
  "license": "MIT",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm -r --filter ./packages/* build",
    "keeper": "pnpm --dir program keeper",
    "keeper:status": "pnpm --dir program keeper:status",
    "lint": "prettier --check \"**/*.{ts,tsx,js,json,md}\"",
    "lint:fix": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
